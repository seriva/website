pre-commit:
  commands:
    format:
      priority: 1
      run: npm run format
      stage_fixed: true

    lint:
      priority: 2
      run: npm run check

    test:
      priority: 3
      run: npm run test:gofront && npm run test:unit
